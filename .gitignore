node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# 実セッションの全記録。hook がプラグイン経由で過去セッションの要約を注入するため
# 版管理に入れない(CLAUDE.md §11)。パーサの門は session-safe.ndjson が担う。
test/fixtures/session-full.ndjson
shots/
coverage/
*.tsbuildinfo
scripts/probe-team*.result.json

# 7 手の走行の絵。個人のパスと私的な PR の URL が写るので版管理に入れない（pnpm walk が撮り直す）
docs/v1-walk/*.png
