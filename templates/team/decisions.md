# 決めたこと

追記のみ。書き換えない。見出しの形は変えない（Izuna が読む）。

## 2026-09-07T15:15:00+09:00 · 01 · brain

説明が切れるのは受け入れる。パレットは名前で選ぶもので、説明は補助情報だから。
ただし省略記号は要る。

それより `argumentHint` が右端で押し出されているのが問題。`flex-shrink: 0` にする。
